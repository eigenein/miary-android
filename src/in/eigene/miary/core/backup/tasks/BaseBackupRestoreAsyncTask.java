package in.eigene.miary.core.backup.tasks;

import android.os.*;
import in.eigene.miary.core.backup.*;

/**
 * Base task for backup and restore.
 */
public abstract class BaseBackupRestoreAsyncTask extends AsyncTask<Void, BackupProgress, BackupResult> {
    // TODO.
}
